export type SoundType = {
  src: string;
  title: string;
  icon: string;
};

export type SoundItemType = {
  src: string;
  title: string;
  icon: string;
};

export type SoundListType = {
  sounds: SoundItemType[];
};
